{
  "name": "unichat-widget",
  "version": "0.1.0",
  "private": true,
  "description": "UniChat embeddable chat widget. Source bundle, output committed to src/static/widget.js.",
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.js",
    "watch": "node esbuild.config.js --watch"
  },
  "dependencies": {
    "dompurify": "^3.2.6",
    "marked": "^15.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
